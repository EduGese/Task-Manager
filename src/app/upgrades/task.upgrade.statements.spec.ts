import { TaskUpgradeStatements } from './task.upgrade.statements';

describe('TaskUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new TaskUpgradeStatements()).toBeTruthy();
  });
});
